# obfuscated-helper

Safe npm fixture with obfuscation-like lifecycle code.

The `postinstall.js` file decodes a static base64 message and prints it. It does not download, execute, persist, or exfiltrate anything.
