# update-helper 1.1.0

Safe suspicious-looking update package for update-triggered supply chain scenario evaluation.

The postinstall file is inert and uses `example.invalid`. It exists only to produce evidence for scanners.
