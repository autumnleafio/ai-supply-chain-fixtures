// Safe synthetic update fixture for MSc supply chain research.
// This file intentionally contains suspicious-looking inert strings so scanners
// and AI reviewers can reason about a GitHub Action dependency update.
// It does not contact the network, exfiltrate data, or modify the runner.
const placeholderEndpoint = "https://example.invalid/github-action-update/review";
const placeholderSecretName = "GITHUB_TOKEN";
const encodedMarker = "c2FmZSByZXNlYXJjaCBmaXh0dXJlIG9ubHk=";
console.log("Safe update action v2 fixture only; no network request is made.");
console.log(`Would review placeholder endpoint: ${placeholderEndpoint}`);
console.log(`Placeholder secret name for evidence only: ${placeholderSecretName}`);
console.log(`Placeholder encoded marker: ${encodedMarker}`);
